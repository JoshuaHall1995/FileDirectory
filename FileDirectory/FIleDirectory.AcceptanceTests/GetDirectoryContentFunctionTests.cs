using System;

namespace FIleDirectory.AcceptanceTests
{
    public class GetDirectoryContentFunctionTests
    {
        // This would be used to test the real endpoint upon release. Build up real Http requests and assert on
        // responses. To ensure they are totally decoupled from the rest no shared models should be used. Using
        // something like Cucumber could work for this depending on pipeline setup. Test cases should be written
        // with BDD in mind and cover the expected scenarios. 
    }
}